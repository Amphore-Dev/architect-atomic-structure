# Architect Plugin: <plugin_name>

This plugin adds <plugin_name> generation functionality to the Architect CLI tool.

## Installation

Install the plugin in your project directory:

```bash
npm install architect-plugin-<plugin_name>
```
